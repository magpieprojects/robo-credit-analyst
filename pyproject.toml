[project]
name = "robo-credit-analyst"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.129.0",
    "openai>=2.21.0",
]

[project.optional-dependencies]
dev = [
    "uvicorn>=0.40.0",
]
